# 复制为 .env.local 后修改；勿提交密钥。
#
# 微信开发者工具模拟器：可用 http://127.0.0.1:3001（并勾选不校验合法域名）
# 真机预览：填电脑局域网 IP，例如 http://192.168.1.16:3001（不能用 127.0.0.1）
#
# 修改本文件或 .env.local 后须重新执行 npm run dev:weapp。
# 临时改 IP（不重编）：Console 执行 wx.setStorageSync('DEV_API_BASE','http://192.168.1.16:3001') 后重进小程序。
TARO_APP_API_BASE=http://127.0.0.1:3001

# TRTC、百炼 DashScope（出题/分段 ASR）均在项目根目录 .env.local 配置，见仓库根 .env.example

# 候选人流程调试日志（微信开发者工具 Console）：设为 1 后重新 npm run dev:weapp
# 或运行时 wx.setStorageSync('FLOW_DEBUG','1') 后重进小程序
# TARO_APP_FLOW_DEBUG=1

# 面试页语音转写：微信插件 WechatSI（app.config 已声明插件时需在开发者工具添加该插件）

# 若仍使用面试官端 VoIP 兜底，可取消注释：
# TARO_APP_VOIP_FALLBACK_INTERVIEWER_OPENID=oXXXX

# 面试页「AI 面试官」头像（火山 TOS 等 HTTPS 地址）。不设则使用默认公开对象 URL。
# 微信小程序需在后台配置 downloadFile 合法域名：xiang-xian-miniprogarm.tos-cn-shanghai.volces.com
# 预签名 URL 会过期，仅适合调试；线上请用桶/对象的公开读或服务端下发新链接。
# TARO_APP_AI_INTERVIEWER_IMG_URL=https://xiang-xian-miniprogarm.tos-cn-shanghai.volces.com/static/ai-interviewer.png
